---
import MediaCard from './MediaCard.astro';
import type { LifestyleEntry } from '@/lib/content';
import { formatDate } from '@/utils/content';

interface Props {
  entry: LifestyleEntry;
  large?: boolean;
}

const { entry, large = false } = Astro.props;
const meta = `${entry.data.contentType === 'external' ? '我们发现的事物' : '原创故事'} / ${formatDate(entry.data.date)}`;
---

<MediaCard
  title={entry.data.title}
  href={`/lifestyle/${entry.slug}`}
  imageKey={entry.data.coverImage}
  alt={entry.data.coverAlt}
  meta={meta}
  excerpt={entry.data.personalNote ?? entry.data.excerpt}
  large={large}
/>
